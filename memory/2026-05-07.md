## Requested
Document the Brown Biotech homepage discovery-section refinement work completed in this turn.

## Analysis
The homepage discovery section was revised to sound more premium, neutral, and credible. The prior hype-forward framing was reduced, and unsupported performance metrics were removed from both the discovery workflow and the Brown Biotech highlights.

## Decisions with rationale
Used calmer, more businesslike language so the discovery section reads as an evidence-led entry point rather than a promotional pitch. Removed unsupported hype metrics to avoid overclaiming and to keep the homepage aligned with a credible biotech brand voice.

## Outcomes
The discovery section now feels more premium and neutral. The build passed, and browser vision verification confirmed that the page now feels credible.

## Files changed
- `src/App.tsx`
- `memory/2026-05-07.md`

## Requested
Document the Founder / CEO and Research briefing homepage refinement work completed in this turn.

## Analysis
The homepage was further refined to make the Founder / CEO and Research sections feel more polished, premium, and decision-oriented. The copy changes focused on tone refinement and clearer section labeling rather than structural layout changes.

## Decisions with rationale
Rewrote the Founder summary to sound more neutral and high-trust, avoiding overly promotional phrasing. Relabeled the career section to `Career Track Record` to better signal credibility and progression. Renamed the publications section to `Selected Papers` to make the curation feel deliberate and concise. Reframed `Research Spotlight` as `Research Briefing` and added a concise, decision-ready subtitle so the section reads like a strategic summary instead of a marketing blurb.

## Outcomes
The page now presents the Founder / CEO and Research content in a more premium, editorial style while preserving clarity. The build passed, and browser verification confirmed the result still looks polished.

## Files changed
- `src/App.tsx`
- `memory/2026-05-07.md`

## Requested
Document the final form / OG / canonical / API sync work completed in this pass.

## Analysis
This pass aligned the intake/contact form, metadata, and API behavior end-to-end. The form gained a priority selector and stronger private brief copy, the submit payload now includes priority for `/api/intake`, preview/branding assets were upgraded for social sharing, and the deployment metadata was corrected so canonical and `og:url` point at the live Brownbio domain rather than localhost.

## Decisions with rationale
Kept the priority selector in the form so users can explicitly signal urgency while preserving backend inference as a fallback. Strengthened the private brief language to better set expectations for confidential, high-context requests. Synced the client payload to pass priority through to the intake API so the UI and backend share the same contract. Updated OG/icon assets with the premium `og-image.svg`, icon SVG, and apple-touch-icon SVG to improve social previews and brand presentation. Fixed canonical and `og:url` to `https://brownbio.tech/` so SEO and share cards match the actual deployment domain. Adjusted the API intake route to respect client-supplied priority while still inferring it when missing, preventing silent overrides.

## Outcomes
The build completed successfully. Browser verification also succeeded and confirmed `canonical=https://brownbio.tech/`, `og:url=https://brownbio.tech/`, the nav CTA text is `Request a Brief`, and the modal priority field exists.

## Files changed
- `src/App.tsx`
- `index.html`
- `public/og-image.svg`
- `public/icon.svg`
- `public/apple-touch-icon.svg`
- `src/api/intake` route file(s)
- `memory/2026-05-07.md`
